# Инструкция по загрузке проекта на GitHub

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите на иконку "+" в верхнем правом углу и выберите "New repository"
3. Заполните данные:
   - **Repository name:** `iku-uunit-map` (или другое название)
   - **Description:** "Интерактивная карта кампуса УУНИТ - веб-приложение для навигации по университету"
   - **Visibility:** Public (если хотите, чтобы все видели) или Private (если только для вас)
   - **Initialize this repository with:** НЕ выбирайте ничего
4. Нажмите "Create repository"

## Шаг 2: Подготовка локального репозитория

Откройте терминал и выполните следующие команды:

```bash
# Перейдите в папку проекта
cd /home/ubuntu/uust_map_prototype

# Инициализируйте Git репозиторий (если еще не инициализирован)
git init

# Добавьте все файлы в индекс
git add .

# Создайте первый коммит
git commit -m "Initial commit: ИКУ УУНИТ - интерактивная карта кампуса"

# Добавьте удаленный репозиторий GitHub
# Замените USERNAME на ваше имя пользователя GitHub
# Замените REPOSITORY на название репозитория
git remote add origin https://github.com/USERNAME/REPOSITORY.git

# Переименуйте ветку на main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

## Шаг 3: Если у вас уже есть SSH ключ

Если вы используете SSH вместо HTTPS:

```bash
git remote add origin git@github.com:USERNAME/REPOSITORY.git
git push -u origin main
```

## Шаг 4: Структура проекта на GitHub

После загрузки ваш репозиторий будет содержать:

```
iku-uunit-map/
├── client/                 # React фронтенд
│   ├── src/
│   ├── public/
│   └── package.json
├── server/                 # Node.js бэкенд
│   ├── db.ts
│   ├── routers.ts
│   └── ...
├── drizzle/               # База данных схема
│   └── schema.ts
├── PRESENTATION_SPEECH.md # Текст для выступления
├── GITHUB_SETUP.md        # Эта инструкция
├── README.md              # Описание проекта
└── package.json
```

## Шаг 5: Создание README.md

Создайте файл `README.md` в корне проекта с описанием:

```markdown
# ИКУ УУНИТ - Интерактивная карта кампуса

Веб-приложение для навигации по кампусу Уфимского университета науки и технологий.

## Функциональность

- 🗺️ Интерактивная карта кампуса с 9 корпусами
- 📝 Редактор полигонов для администраторов
- 🏢 Конструктор макетов корпусов
- 👥 Разделение прав доступа (администратор/пользователь)
- 🌐 Полная локализация на русском языке

## Технологический стек

**Фронтенд:**
- React 19
- Tailwind CSS 4
- Leaflet.js (для карт)
- TypeScript

**Бэкенд:**
- Node.js
- Express 4
- tRPC 11
- MySQL (Drizzle ORM)

## Установка и запуск

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/USERNAME/iku-uunit-map.git
cd iku-uunit-map
```

2. Установите зависимости:
```bash
pnpm install
```

3. Создайте файл `.env` с переменными окружения

4. Запустите dev сервер:
```bash
pnpm dev
```

5. Откройте http://localhost:3000

## Демо учетные данные

- **Администратор:** admin@uust.edu / password123
- **Пользователь:** user@uust.edu / password123

## Авторы

- Гарифуллин Искандер Рамилевич - разработка
- Фаттахов Айдар Альбертович - дизайн
- Чезганов Вадим Максимович - концепция и дизайн

## Лицензия

MIT

## Контакты

- Email: iskanderGarif@gmail.com
- Email: vadimch@mail.ru
```

## Шаг 6: Обновление кода на GitHub

Когда вы внесете изменения в проект:

```bash
# Добавьте изменения
git add .

# Создайте коммит с описанием
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

## Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю коммитов
git log

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# Создать новую ветку
git checkout -b feature/название

# Переключиться на ветку
git checkout main

# Слить ветку в main
git merge feature/название
```

## Решение проблем

### Ошибка: "fatal: not a git repository"
Выполните `git init` в папке проекта

### Ошибка: "Permission denied (publickey)"
Убедитесь, что у вас настроены SSH ключи для GitHub

### Ошибка: "The remote repository already exists"
Удалите старый remote: `git remote remove origin`

## Дополнительно

После загрузки на GitHub вы можете:
- Включить GitHub Pages для автоматического развертывания
- Настроить CI/CD через GitHub Actions
- Добавить Issues и Pull Requests для управления разработкой
- Пригласить других разработчиков в проект

Удачи с проектом! 🚀
